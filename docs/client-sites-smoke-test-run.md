# Client Sites — Smoke Test Run Log

**Phase:** 3C — Safe Smoke Test Run Log
**Date:** 2026-05-20
**Scope:** DB-level smoke + public `/s/:token` browser smoke (no real login / no session switching)
**Status:** PASS for safe scope. Manual real-account E2E pending.

---

## 1. Test Data Summary

### Primary Test Site
| Field | Value |
|-------|-------|
| site_ref | `STE-2026-100001` |
| internal id | `897cd2b6-…` |
| site_name | `QA Smoke Villa` |
| site_type | `villa` |
| city | `Riyadh` |
| district | `Al-Olaya` |
| owner_user_id | `1bbb53db-…` (Hashem, super_admin) |
| linked business | `Al Nokhba Aluminum Factory` |
| is_demo | `true` |
| initial visibility | `shared_by_qr` |

### Smoke Tokens Used
| Token | Status | Notes |
|-------|--------|-------|
| `qa-smoke-token-001` | revoked during rotation | initial token |
| `qa-smoke-token-002` | revoked during rotation | post-rotate token |
| `qa-smoke-token-003` | final scan → then revoked | used for browser smoke test |

### Test Environment
- Published URL: `https://qitaat.lovable.app`
- Public scan path: `/s/:token`
- Invalid token used for negative test: `invalid-bogus-token-xyz`

---

## 2. DB-Level Smoke Results

All checks executed via direct database queries (no UI login required).

### 2.1 Site Creation & Defaults
| Check | Result |
|-------|--------|
| `site_ref` generated by trigger matches `STE-YYYY-NNNNNNN` | PASS |
| `site_type` stored correctly | PASS |
| `visibility` default = `shared_by_qr` | PASS |

### 2.2 Section Visibility Seeding
| Check | Result |
|-------|--------|
| `tg_seed_client_site_visibility` fires on INSERT | PASS |
| 20 `client_site_visibility` rows created | PASS |
| `site_ref`, `site_type`, `city` → `public_limited` | PASS |
| `address`, `map_url`, `contact_phone` → `visible_to_approved_provider` | PASS |
| `specs`, `measurements`, `attachments`, `photos` → `visible_after_approval` | PASS |
| `contracts`, `visits`, `notes` → `admin_only` | PASS |

### 2.3 Notification Preferences Seeding
| Check | Result |
|-------|--------|
| `tg_seed_client_site_notif_prefs` fires on INSERT | PASS |
| 1 row created with defaults (`provider_interest=true`, `access_request=true`, etc.) | PASS |

### 2.4 `get_public_site_by_token` (Anonymous Read)
| Check | Result |
|-------|--------|
| Returns only 6 safe keys (no PII leak) | PASS |
| Returned keys: `site_ref`, `site_name`, `site_type`, `city`, `district`, `visibility` | PASS |
| Does NOT return: `address`, `phone`, `map_url`, `qr_token_hash`, `scan_count`, `owner_user_id` | PASS |

### 2.5 Visit Logging
| Check | Result |
|-------|--------|
| `scan_count` incremented on each call | PASS |
| `client_site_visit_logs` row created | PASS |
| `client_site_lookup_audit` row created | PASS |

### 2.6 QR Token Lifecycle
| Check | Result |
|-------|--------|
| Token hash mapped correctly | PASS |
| Rotate: old token → `NULL`, new token → summary | PASS |
| Revoke (`qr_enabled=false`): all tokens → `NULL` | PASS |
| Bogus/empty tokens → `NULL` (no crash) | PASS |

---

## 3. Public `/s/:token` Browser Smoke Results

All checks executed via browser automation on published URL (anonymous session).

### 3.1 Valid Token (`qa-smoke-token-003`)
| Check | Result |
|-------|--------|
| Page loads without errors | PASS |
| Displays limited summary card | PASS |
| Shows: reference number, name, city, type | PASS |
| Locked placeholders for access-restricted sections | PASS |
| Arabic UI renders correctly | PASS |
| No console errors (excluding third-party tracker 403) | PASS |

### 3.2 Invalid/Revoked Token (`invalid-bogus-token-xyz`)
| Check | Result |
|-------|--------|
| Generic unavailable state displayed | PASS |
| No stack trace or internal error leaked | PASS |
| Arabic "unavailable" message shown | PASS |
| No console errors | PASS |

---

## 4. Privacy Verification

| PII / Sensitive Field | Expected | Verified |
|-----------------------|----------|----------|
| `123 Test Street` (address) | hidden | hidden |
| `+966500000000` (phone) | hidden | hidden |
| `maps.google.com` (map URL) | hidden | hidden |
| `24.7136` (coordinates) | hidden | hidden |
| `Hashem` (owner name) | hidden | hidden |
| `qr_token_hash` | hidden | hidden |
| `scan_count` | hidden | hidden |
| `owner_user_id` | hidden | hidden |
| site UUID `897cd2b6-…` | hidden | hidden |

### Meta / Indexing
| Check | Result |
|-------|--------|
| `useNoIndex()` hook wired in `PublicSiteScan.tsx` | PASS |
| `/robots.txt` Disallow `/s/` | PASS |
| sitemap exclusion for `/s/` paths | PASS |

---

## 5. What Was Skipped and Why

The following RPCs and UI flows were **intentionally skipped** because they require authenticated sessions (owner or provider login) which this safe smoke test does not perform.

| Skipped Item | Why Skipped | Covered By |
|--------------|-------------|------------|
| `issue_client_site_qr_token` | requires owner JWT | DB trigger/audit confirmed |
| `rotate_client_site_qr_token` | requires owner JWT | DB trigger/audit confirmed |
| `revoke_client_site_qr_token` | requires owner JWT | DB trigger/audit confirmed |
| `request_client_site_access` | requires provider JWT + real scan | Manual E2E checklist |
| `submit_site_interest` | requires provider JWT + access grant | Manual E2E checklist |
| `list_site_access_requests_for_owner` | requires owner JWT | Manual E2E checklist |
| `set_client_site_visibility` | requires owner JWT | DB default confirmed |
| `update_site_section_visibility` | requires owner JWT | DB seed confirmed |
| `get_client_site_notification_preferences` | requires owner JWT | DB seed confirmed |
| `update_client_site_notification_preferences` | requires owner JWT | DB seed confirmed |
| Owner inbox UI render with real rows | requires owner login | Manual E2E checklist |
| Provider dashboard → scan → request flow | requires provider login | Manual E2E checklist |
| Contract creation + snapshot enforcement | requires approved provider | Manual E2E checklist |
| Arabic PDF export visual check | requires real contract | Manual E2E checklist |

---

## 6. Manual Real-Account E2E Checklist

The following must be completed by a human with real owner and provider accounts.

### Owner Account Steps
- [ ] Log in as owner (`Hashem` / super_admin)
- [ ] Navigate to Client Sites → Create new site
- [ ] Verify `site_ref` auto-generates correctly
- [ ] Issue QR token from UI
- [ ] Copy QR token / download QR image
- [ ] Rotate QR token — confirm old token invalidates
- [ ] Revoke QR token — confirm site becomes unavailable

### Provider Account Steps
- [ ] Log in as a provider user
- [ ] Scan public `/s/:token` page (or enter token manually)
- [ ] Click "Request Access" — submit request form
- [ ] Verify access request appears in owner inbox
- [ ] Submit provider interest / quotation request
- [ ] Verify interest notification reaches owner

### Owner Approval & Contract
- [ ] Owner opens inbox / access requests
- [ ] Approve provider access request
- [ ] Confirm provider can now view unlocked sections (address, phone, map)
- [ ] Create contract from site snapshot
- [ ] Verify contract pulls correct site data
- [ ] Confirm contract snapshot is locked upon "Active" status

### PDF Export
- [ ] Export contract as PDF
- [ ] Verify Arabic text renders correctly (Amiri font)
- [ ] Verify no PII leaks in PDF (address/phone only if approved)
- [ ] Visual check: layout, RTL, page breaks

### Notifications
- [ ] Owner receives email/push for provider interest
- [ ] Owner receives email/push for access request
- [ ] Provider receives email/push for approval/denial
- [ ] Update notification preferences — confirm persistence

### Visibility Settings
- [ ] Owner changes visibility from `shared_by_qr` to `public_limited`
- [ ] Verify public scan page shows/hides sections accordingly
- [ ] Owner changes section-level visibility
- [ ] Confirm granular section visibility respected on public page

---

## 7. Final Status

| Scope | Status |
|-------|--------|
| DB-level smoke (triggers, defaults, audit, privacy) | **PASS** |
| Public `/s/:token` browser smoke (valid + invalid tokens) | **PASS** |
| Privacy verification (no PII in public DOM) | **PASS** |
| No console errors on public scan page | **PASS** |
| Code changes required | **NONE** |
| DB / RLS / auth changes required | **NONE** |
| Manual real-account E2E | **PENDING** |

### Overall
**PASS** for the safe smoke test scope.
Manual real-account end-to-end test remains as a follow-up.

---

*Document generated automatically from Phase 3B smoke test results.*

## Admin QR Manager (Audited Rotate)

QA-seeded sites only store `qr_token_hash` — the raw QR token cannot be
reconstructed. To get a visible/printable QR from `/admin/client-sites`:

1. Open the site detail drawer.
2. Expand "Site QR management".
3. Enter a reason (≥ 5 chars) and click "Generate new QR".
4. The new token is shown **once** — copy URL, download PNG, or print sticker.
5. The previous token is invalidated. An `admin_client_site_access_audit` row
   with `action = 'qr_token_rotated'` is written.

If the site `visibility` is `private`, the new QR is generated but `/s/:token`
will not resolve until visibility is changed to `shared_by_qr`.
