/** Shared StatusBadge — see `AdminStatusBadge`. Use everywhere for status chips. */
export { AdminStatusBadge as StatusBadge, default } from '@/components/admin/AdminStatusBadge';
export type { AdminStatusTone as StatusTone } from '@/components/admin/AdminStatusBadge';