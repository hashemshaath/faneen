/**
 * Typed Supabase RPC wrappers.
 *
 * Status: Scaffold only (R0). No wrappers implemented yet.
 * In R1, helpers such as `callHasRole`, `callForceSaudiCountryBeta` (read check),
 * and barcode lookup RPCs will live here. Until then, existing direct
 * `supabase.rpc(...)` calls continue to work unchanged.
 */
export {};
