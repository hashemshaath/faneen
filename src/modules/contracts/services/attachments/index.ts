/**
 * CT-7 — Contract runtime attachment service barrel.
 * Storage helpers + bucket constant. Table CRUD lives in
 * `services/childTables/attachments.ts` (CT-4).
 */
export * from './constants';
export * from './storage';