/**
 * R2A.1 — Contracts module public surface for the pricing engine
 * (per-line totals across pricing methods). Kept separate from
 * financials.ts per R2A.1 directive to avoid mixing concerns.
 */
export * from '@/lib/contract-pricing';