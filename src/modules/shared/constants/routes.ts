/**
 * Public route prefixes (R1A scaffold).
 * Not yet imported by production routes; tests may consume these to assert
 * stability of the public surface.
 */
export const PUBLIC_BARCODE_ROUTE_PREFIX = '/q' as const;
export const PUBLIC_SITE_TOKEN_ROUTE_PREFIX = '/s' as const;