/**
 * /search — V3 entrypoint.
 * The page implementation lives in `SearchV3.tsx`. This file is kept as the
 * router-stable shell so `App.tsx` and any deep links continue to resolve.
 */
export { default } from './SearchV3';
