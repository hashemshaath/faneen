/**
 * Legacy private-sectors catalog (moved from `/brands`).
 * Lives at `/private-sectors`. The `/brands` route now hosts the
 * new Brands Governance public registry.
 */
export { default } from './BrandsCatalogLegacy';