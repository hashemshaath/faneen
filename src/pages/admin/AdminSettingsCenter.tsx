/**
 * ADMIN UX RECONSOLIDATION PHASE 2 — Settings Center shell.
 *
 * Currently re-exports the existing System Settings hub so the new
 * canonical /admin/settings URL resolves without moving any logic.
 */
export { default } from './AdminSystemSettingsHub';
