{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "//": "Knip config for Qitaat. Tuned to surface real orphan code by declaring every dynamic / out-of-bundler entry point. Do not widen 'ignore' to swallow real signal — add a precise entry instead.",
  "entry": [
    "src/main.tsx",
    "index.html",
    "vite.config.ts",
    "vitest.config.ts",
    "playwright.config.ts",
    "playwright.cross-browser.config.ts",
    "playwright-fixture.ts",
    "tailwind.config.ts",
    "postcss.config.js",
    "eslint.config.js",
    "src/test/setup.ts",
    "src/**/*.{test,spec}.{ts,tsx}",
    "src/**/__tests__/**/*.{ts,tsx}",
    "e2e/**/*.spec.ts",
    "scripts/**/*.{mjs,ts,js}",
    "//": "Each Supabase Edge Function is its own deploy entry point (Deno runtime).",
    "supabase/functions/*/index.ts",
    "supabase/functions/**/*_test.ts",
    "supabase/functions/**/*.test.ts"
  ],
  "project": [
    "src/**/*.{ts,tsx}",
    "supabase/functions/**/*.{ts,tsx}",
    "scripts/**/*.{mjs,ts,js}"
  ],
  "ignore": [
    "src/integrations/supabase/types.ts",
    "src/integrations/supabase/client.ts",
    "src/components/ui/**",
    "//": "shadcn primitives — kept by design for future composition.",
    "src/modules/*/index.ts",
    "//": "Module barrels — public API surface for cross-module imports.",
    "supabase/functions/_shared/transactional-email-templates/**",
    "supabase/functions/_shared/email-templates/**",
    "//": "Email templates are dispatched dynamically by name from send-transactional-email / auth-email-hook.",
    "supabase/functions/_shared/email-layout/**",
    "supabase/functions/_shared/**",
    "//": "Edge shared modules are imported via relative paths the Knip TS resolver does not fully crawl across function boundaries."
  ],
  "ignoreDependencies": [
    "//": "Tooling consumed by config files / vite plugins / lovable runtime that Knip can miss.",
    "@types/.*",
    "autoprefixer",
    "postcss",
    "tailwindcss-animate"
  ],
  "ignoreBinaries": ["pdftotext", "deno", "supabase"],
  "rules": {
    "duplicates": "off",
    "enumMembers": "off"
  }
}